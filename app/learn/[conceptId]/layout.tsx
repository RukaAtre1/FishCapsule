export const metadata = {
  title: "Concept | AI Self-Study Capsule",
  description: "Concept detail and Cornell card."
};

export default function ConceptLayout({ children }: { children: React.ReactNode }) {
  return children;
}
