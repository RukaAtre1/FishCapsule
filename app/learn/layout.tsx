export const metadata = {
  title: "Learn | AI Self-Study Capsule",
  description: "Review and practice generated Cornell cards."
};

export default function LearnLayout({ children }: { children: React.ReactNode }) {
  return children;
}
